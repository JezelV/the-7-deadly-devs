/*
    ? Paso 1 crear una funcion guardarLocal() que tenga como proposito guardar los datos proporcionados por el usuario localmente en el navegador. que reciba como parametros:


    * Dato = El dato que se quiere guardar localmente en el navegador

    * llave = la referencia que va a tener que se va a hacer al dato local




*/